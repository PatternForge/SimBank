name: 'simbank_dbt'
version: '6.0.0'
profile: 'simbank_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  simbank_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
